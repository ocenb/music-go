apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-service-deployment
spec:
  template:
    spec:
      containers:
        - name: content-service
          env:
            - name: DOMAIN
              value: api.music-go.example.com
