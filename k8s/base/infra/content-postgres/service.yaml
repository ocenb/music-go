apiVersion: v1
kind: Service
metadata:
  name: content-service-postgres
  labels:
    app: content-postgres
spec:
  selector:
    app: content-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
